---
const { name, symbol, atomicNumber, percentageInHumans } = Astro.props;
---

<div class="element-block bg-white p-4 rounded-lg shadow-md m-4 max-w-xs">
    <div class="flex items-center justify-between mb-2">
        <h2 class="text-2xl font-bold text-indigo-900">{name}</h2>
        <span class="text-xl font-semibold text-gray-700">{symbol}</span>
    </div>
    <p class="text-gray-600 mb-2">Atomic Number: {atomicNumber}</p>
    <p class="text-gray-700">Percentage in Humans: {percentageInHumans}%</p>
</div>